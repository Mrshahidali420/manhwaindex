{
  "name": "manhwaindex",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "dev": "astro dev",
    "build": "node scripts/make-redirects.mjs && astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
